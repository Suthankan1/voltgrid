type Query {
    stationStatus(stationId: ID!): StationStatus

    stationStatuses(
        first: Int
        after: String
    ): StationStatusConnection!
}

type StationStatus {
    stationId: ID!
    currentStatus: String!
    lastEventId: ID!
    statusChangedAt: String!
    updatedAt: String!
}